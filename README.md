# Chat-J
## How to use
### create virtual environment if needed
`python -m venv venv`

`source venv/bin/activate`
### install libraries
`pip install -r requirements.txt`
### run flask server
`flask run`
